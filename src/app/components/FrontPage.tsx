'use client';
import React from 'react';
import TitleText from './TitleText';

const FrontPage = () => {
    return (
        <section className='flex sm:flex-col sm:items-center justify-center h-screen'>
            <TitleText name='Noel Ohaeri' />
        </section>
    );
};

export default FrontPage;
